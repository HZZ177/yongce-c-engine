得到用户token
通过数据库得到用户的速停车accessToken
---
tags:
  - 出入车 API
parameters:
  - name: env
    in: query
    type: string
    required: false
    description: 默认test，test：测试环境，pro:生产环境
  - name: userId
    in: query
    type: string
    required: true
    description: 用户id

responses:
  500:
    description: 操作失败
  200:
    description: 操作成功